spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/st4911/config-properties.git
          search-paths: configs
      label: master

server:
  port: 8888